# Triathlon-manager-react-dotnet
Full-stack event platform built with React and Java Spring Boot (adaptable to .NET). Features secure JWT authentication, relational SQLite storage, and live referee updates via WebSockets.
